package com.groove.music.ui.theme

import androidx.compose.ui.graphics.Color

// ── Groove brand palette — deep purple/violet + electric accents ──────────

// Primary brand colors
val GroovePurple       = Color(0xFF7C3AED)   // Primary violet — mirrors --accent in web CSS
val GroovePurpleLight  = Color(0xFFA78BFA)   // Light violet   — mirrors --accent-light
val GroovePurpleDim    = Color(0x337C3AED)   // Dim with alpha — mirrors --accent-dim

// Accent
val GrooveBlue         = Color(0xFF0EA5E9)   // Sky blue — download/stream actions
val GrooveGreen        = Color(0xFF22C55E)   // Success / in library
val GrooveRed          = Color(0xFFEF4444)   // Error / delete
val GrooveAmber        = Color(0xFFF59E0B)   // Warning / needs upload

// Dark surface colors (mirrors the dark glassmorphic CSS variables)
val GrooveBg           = Color(0xFF0A0A0F)   // App background
val GrooveSurface      = Color(0xFF13131A)   // Card / panel surface
val GrooveSurfaceHigh  = Color(0xFF1C1C28)   // Elevated surface
val GrooveBorder       = Color(0xFF2A2A3A)   // Border color

// Text colors
val GrooveTextPrimary  = Color(0xFFF8F8FF)
val GrooveTextMuted    = Color(0xFF9090A8)
val GrooveTextSubtle   = Color(0xFF5A5A72)

// Player gradient colors
val GroovePlayerGradStart = Color(0xFF1C1C2E)
val GroovePlayerGradEnd   = Color(0xFF0A0A14)
