package com.groove.music

import org.junit.Test
import org.junit.Assert.*

/**
 * Placeholder unit test — required so Gradle's testClasses task has
 * a source set to compile against.
 * Add real tests here as the app grows.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
